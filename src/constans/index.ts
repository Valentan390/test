// import { resolve } from 'path';
import { resolve } from 'node:path';

export const TEMPLATES_DIR = resolve('src', 'templates');

export const TEMP_UPLOAD_DIR = resolve('temp');

export const UPLOAD_DIR = resolve('uploads');
